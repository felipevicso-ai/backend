PORT=3000

# SQLite (mude para um caminho persistente em produção)
DB_PATH=./src/db/onlive.db

# Cakto
CAKTO_WEBHOOK_SECRET=

# SMTP (ex: Resend, SES, Postmark, Zoho, etc)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Onlive <no-reply@seudominio.com>"
